==============
 Introduction
==============

``exponent`` is an opinionated library for building services with a
fractal architecture built on top of Twisted. All data is stored in
Axiom stores. Clients interact with the server using the AMP RPC
protocol.
